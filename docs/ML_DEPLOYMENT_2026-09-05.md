# ML 升级部署回执 · 2026-09-05

用户确认 Claude 修复通过并授权合入 main、推送、重启本机 8888 和执行 ML 发布。工程合并提交为 `99848c9`，包含审核修复 `7d95fc7`；已推送 `origin/main`。README 冲突采用保留 main `885e8f7` 修订的审核版解决，合并代码树与审核版本一致。

## 实际执行

1. 在原运行目录 `data/deployments/20260905-ml-upgrade/` 保存切换前源码、配置和两库在线备份；复制恢复、integrity_check、schema 及版本幂等迁移演练通过。
2. 初始化运行库新 schema，294 条旧预测迁入审计版本；原主库持仓、订单、成交和行情内容核对不变。
3. 8888 服务已从 main 运行目录重启。首页、`/ml-next`、`/ml-next/` 均 HTTP 200，ML 数据连接只读验证通过。
4. 执行 `ml.sh data`，刷新 ML 日线、小时线和主库事实快照。没有额外执行 update.sh；本轮主库已有截至 9 月 4 日的账户事实，部署没有更改这些事实。
5. 运行库历史审计发现 2026-04-20 三只港股各缺完整小时线；使用此前核验的 Futu 原始 CSV，经导入器重新核对后补齐，保留 source_ref 和回执。重新逐日拟合 720 条历史预测，run `4d02a704680342c78452c2cbd9fdbcd0`，输入冻结在运行目录，训练／校准截止及 SHA256 核验通过。没有整库覆盖运行数据。
6. 执行 `ml.sh train`，run `deploy-20260905`，六股均 generated。备份公网旧 index 后执行 `ml.sh publish data/ml/receipts/deploy-20260905.json`，六股均 published，无过期发布。

## 验收

| 项目 | 结果 |
| --- | --- |
| 六股历史 20 / 60 / 120 session | 分别 120 / 360 / 720 条 ok，行情、预测与小时线无缺口 |
| 新 live 目标日 | HK：2026-09-07；US：2026-09-08 |
| 运行 ML 库 | integrity_check=ok；旧版本保留，recomputed 与 live 分开 |
| 本机页面 | 8888 首页及 ml-next 正常；历史默认包含明确标注的重建 |
| 公网报告 | HTTPS 200，返回内容 SHA256 与本次训练回执完全一致 |
| 工程验证 | 审核版 227 项测试已通过；合并代码树与审核版一致，本轮另完成真实迁移、数据、训练和发布验收 |

本机入口：[ML 页面](http://127.0.0.1:8888/ml-next/)。公网仅发布静态报告：[本次报告](https://g.ismayday.com/mystock/)，没有把本机账户 Web 暴露到公网。

私有备份、采集／训练／发布日志、历史导入证据、验证明细均在运行目录的 `data/deployments/20260905-ml-upgrade/`；训练回执位于 `data/ml/receipts/`，冻结输入与 manifest 位于 `data/ml/runs/`。这些文件、配置和真实账户数据不进入 Git。

此次为工程部署，未更换生产模型、未晋级 E0–E5 候选、未自动下单，也未配置自动任务。证券规则历史、输入快照保留、采集按市场部分失败等延期问题继续按 [Claude 修复回执](ML_UPGRADE_CLAUDE_FIXES_2026-09-05.md) 跟踪。
