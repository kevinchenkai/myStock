# myStock ML 升级执行工单

> 工单：ML-UPGRADE-20260904；负责人：Codex Astra（按用户最新指示，独立执行任务）。
> 状态：已授权启动工程实施；按本工单顺序完成四批代码、测试和离线验证。
> 代码／数据备份基线：`3b80d12c0070a01f1019f9ae46205132b946730e`。
> 方案：[Codex v1.4](ML_CODEX_UPGRADE_PLAN_2026-09-04.md)；讨论：[Claude v0.2.1](ML_CLAUDE_UPGRADE_MERGED.md)；恢复依据：[升级前备份记录](ML_PRE_UPGRADE_BACKUP_2026-09-04.md)。

## 1. 目标和完成边界

为当前六只美股／港股生成下一交易日可追溯的高低预测及固定规则模拟报价，帮助人类低买高卖，并用有现金／库存／费用约束的「ML 挂单回溯」评价周期权益。预测覆盖区间与执行报价分开验证。

本次授权完成四批工程和隔离数据上的离线实验；不能只写计划或完成第一批就结束。某个模型无增益时记录负结果，继续完成执行引擎与页面。业务参数不明时按 §6 实现显式输入和测试场景，不阻塞工程。

交付是可审查的工作树分支、分批提交、测试／实验报告和可供用户预览的结果。**不自动合入 main，不对当前运行目录做数据迁移，不重启当前 Web，不发布公网报告，不操作交易，不新增 cron／launchd，不等待或伪造 60 个未来交易日。** 前向 shadow、实际策略参数确认与生产切换作为后续阶段明确报告。

若与旧讨论中的“四列版本化”“资金流满两年”“统一港股 100 股”“02:40 定时批次”等说法冲突，以本工单和 Codex v1.4 的最新范围为准。技术细节可在保持不变量时合理调整，说明变更依据，不为常规实现选择重复请求确认。

## 2. 开工条件与数据隔离（必须先完成）

| ID | 执行动作 | 验收证据 |
| --- | --- | --- |
| B00 | 使用新建的 Codex 项目工作树；读取当前 CLAUDE.md、适用 AGENTS.md 和本工单；确认源码基线未被其他任务更改 | 在执行日志记录路径、Git SHA、分支和工作区状态 |
| B01 | 验证私有备份的 `SHA256SUMS`、bundle 和两份数据库 integrity；不修改备份 | 校验通过记录；不打印配置或真实交易内容 |
| B02 | 将备份 `snapshot/data/` 复制到工作树自己的 `data/`，测试／实验路径全部指向此副本；只在确实需要时复制本地配置，不输出内容 | 路径解析证明独立于原运行目录和备份；禁止数据软链接；源库和备份保持不变 |
| B03 | 固定实验输入副本及其哈希；另设可写输出目录，记录依赖版本；开始前运行 §7 的快速回归基线 | 执行日志记录已有失败／跳过原因，不把它们算成本次引入 |

本地备份路径：`/Users/kk/Work/Workpace/GitHub/Seattle/myStock-backups/pre-upgrade-20260904-221530`。

运行中的原项目：`/Users/kk/Work/Workpace/GitHub/Seattle/myStock`。本机测试 Python：`/opt/anaconda3/envs/mk/bin/python`。不要为省事把 cwd 切回原项目执行训练或迁移；实验输出、模型产物和临时数据库全部留在自己的工作树且不提交 Git。

## 3. 第一批：P0 时间守卫与可用性

| ID | 文件范围／任务 | 必须通过的验收 |
| --- | --- | --- |
| P01 | 新增或集中 session 计算模块；`fetch.py / predictor.py / report.py` 使用同一可注入 UTC 时钟与市场日历；包含美港假日、半日市、午休、冬夏令时及适用收盘时段 | 合成时钟测试；不按“下一条现存日线”或固定普通日钟点推断所有交易日；日历缺信息时明确 unavailable |
| P02 | ML 采集仅将完整／确认的日线与小时 bar 纳入最终输入；预测在 build_features 前再次检查；处理已入库但收盘后未更新的盘中缓存 | 盘中行不作 as_of，也不污染前一日标签；收盘后旧缓存仍显示 awaiting_final_data；直接 train 不能绕过 |
| P03 | 人工任意时刻运行：盘中市场跳过，其他市场独立继续；日志记录 skipped_in_session；生成后发布前再检查目标日截止 | 午休视为 session 未结束；训练跨开盘时 missed_deadline；不能顺延旧 target 来伪装有效 |
| P04 | `scripts/ml.sh / report.py` 按本次有效结果推进阶段；部分／全部跳过与失败有结构化状态，失败非零；更新手动流程注释 | 所有跳过不覆盖有效 latest、不重新发布旧报告；部分成功不把其他市场显示为已更新；测试必须 mock 发布命令 |
| P05 | `db.py / data.py / strategy.py / review.py / web/app.py` 增只读连接与注入；`backfill` 读行情透传 db_path、离线写入口仍可写 | Web 尝试 SQL 写入必失败；回填只写指定临时库；ML 不 import Web，不触发在线 API |
| P06 | 固定 N 个市场 session 的窗口；保留 missing_prediction／missing_daily／missing_bars／pending 等状态；参数校验与去重保序 | 删除 fixture 中间一天仍显示一行缺口；无效股票 400，不静默替换；缺 ML 库 503 |
| P07 | `fetch._ohlc_ok` 检查有限值、正价及 OHLC 关系；当前量价数据异常有显式状态 | inf、NaN、high<low、open/close 超界等测试；空抓取不无条件记作成功 |
| P08 | 区分证券 lot/tick 与策略 order_qty；补采集字段及 schema／映射、ML 只读快照读取，或先用带来源与生效信息的配置 | mock Futu 元数据走完整链路；未知规则明确近似，不用成交量推断或把当前规则反填历史；无需本轮实际连接 OpenD |

第一批优先解决 guard，再接入回溯。已存在的错误 HTML／预测证据保留；新拒绝／跳过记录日志，不为了演示版本化刻意生成无效 live 预测。发布代码可以改并测试，执行任务不能实际调用 scp／ssh 发布报告。

## 4. 第二批：不可覆盖留档与可复现实验

| ID | 文件范围／任务 | 必须通过的验收 |
| --- | --- | --- |
| V01 | `schema.sql / db.py` 新增唯一一张 `ml_prediction_versions`；prediction_id＋run_id/code/as_of/target 唯一；幂等冲突检测 | 同日多次生成并存；同 run 相同内容重试幂等，不同内容报冲突；不用 REPLACE 覆盖旧版本 |
| V02 | `data/ml/runs/<run_id>/manifest.json` 记录 Git／依赖／特征／模型／seed、训练校准截止、输入哈希与可恢复路径、状态 | 每条预测可追到 manifest；generated／decision／published 分开；旧未知字段为 NULL／unknown |
| V03 | report／backfill 统一版本化写入口；旧表作明确选择规则的兼容投影；只在数据副本演练迁移 | 迁移可重试；recomputed／backfill 不替换 live；旧错误报告存为审计状态并排除出默认信号／校准，不改写旧原文 |
| V04 | 固化 `naive_vol` 基准与统一评估 runner；raw quantile 损失和 CQR 覆盖／宽度分别计算；skill 分侧输出 | 正尺度下限、零／非有限分母和 NaN 防护；不能把校准边界按原 alpha 的 pinball 当 raw 指标 |
| V05 | CV 尾折覆盖余数；显式 bagging 设置；冻结旧算法和修正协议分别比较 | 全部可评估尾部样本有 OOF；未来可用时间断言；不能将样本／bagging 改变误记成纯模型增益 |
| V06 | 在同一冻结副本重建 `scripts/ml_experiments/exp_a_baseline.py`；修正后的 runner 另列协议；B 脚本仅作条件事件诊断 | 报告实际 alpha、样本掩码／日期、折权重、种子和差异；B 不成熟期限为 pending，不以独立盈利回合证明组合收益 |

首期不建其余六张规划表，不移植完整 Qlib／MLOps 框架。历史“live”不等于已证明截止前可用；旧报告中的实际时间必须保留来源和不确定性。

## 5. 第三批：有限的模型消融

先冻结可重复的 E0：现有算法、显式 bagging 对照、naive_vol。按以下顺序开展小范围实验，每次只改变一个机制，使用相同测试日期／样本掩码；先固定 seed=0 排查实现，候选接近门槛或拟晋级时再运行 0–4 五种子。

| ID | 机制 | 有限候选 |
| --- | --- | --- |
| E1 | 训练／校准窗 | 全历史对照、最近 504／756 session；校准尾部 25% 对照、固定 60／120；避免无约束组合搜索 |
| E2 | 标签标准化 | 原生收益率对照、以 T 时刻 vol_20 或 ATR 标准化，正尺度保护、还原价格 |
| E3 | 模型容量 | 保留现有参数；小容量 leaves=7／min_child=50 等固定候选，正则机制与特征分开 |
| E5 | 内部波动特征 | 日线范围／多尺度一组，完整小时线摘要一组；共同窗口单独消融 |
| E4 | 校准适应性 | 固定 q 对照、最近成熟 OOF 残差滚动校准；先完成此对照，ACI 非本单必交付 |

候选开发筛选：等股票权重 raw pinball 相对冻结旧模型改善 ≥5%、相对 naive_vol skill ≥3%、至少 4/6 股票方向改善、无单股恶化 >10%；上下侧分别报告，区间覆盖／宽度按方案 §6.4 检查。门槛是筛选协议，不是收益承诺；配对时间分块区间与种子随机性分开报告。

最近 120 session 属开发评估窗口，记录实际起止和查看次数，不能反复改参后称独立 holdout。候选失败不调低门槛、不无限搜索、不妨碍第四批；冻结旧预测或已验证的简单基准继续使用。

可选：已有资金流做 2–4 个特征的共同窗口消融。**E6 池化／多分位、E7 跳空联合模型、E8 触价收益模型及新增外部采集不属于本单必交付**；记录后续建议即可。不得为这些扩展阻塞核心工程。

## 6. 第四批：受约束回溯与 Web 联动

| ID | 文件范围／任务 | 必须通过的验收 |
| --- | --- | --- |
| S01 | `execution.py` 纯函数事件引擎，复用可用撮合；订单生效／失效、现金／库存及预留、费用、跳空、同 bar 歧义 | 资金库存守恒、禁止裸空／超资金、过期委托不成交；不能从日终余额替代盘中峰值 |
| S02 | inventory 跨日状态机：空仓买、有仓卖／受限加仓、库存上限、预定最长持有和退出、期末盯市 | 缺预测不丢已有仓位；退出不事后使用收盘价；多买入不能重复分配一笔退出；未完成回合保留 |
| S03 | 固定 policy：boundary_inventory、naive_vol_inventory、固定偏移／分位；原 legacy 模式兼容 | 同一日期、初始资产与费用对照；费用后权益含浮盈亏；USD／HKD 分开，不跨币种共享现金 |
| S04 | 初始现金／库存、每笔数量、库存上限、费用 profile、最长持有显式配置 | 测试使用预先固定的合成账户和费用；页面不得把测试值当用户实盘参数。费用未配置显示 gross／费用缺失，不标净收益；缺参数提供输入而非替用户决定投资预算 |
| W01 | `/api/ml/strategy` 显式 inventory 模式＋schema_version；新增 v2 latest／review／compare 三接口 | legacy 默认收益口径不被静默替换；API 只读；预测／政策／输入版本可追踪；无库／缺口／无效参数均有契约测试 |
| W02 | 下一目标日卡片＋风险区间／模拟报价分层＋复盘明细＋20/60/120 session 对照 | 明确 as_of、target、published 与 expired／pending／unavailable；重模拟和连续切片区别；行情粒度歧义可见 |
| W03 | 选中日期 K 线叠加与真实委托事实对照，沿用原生 JS／本地 Lightweight-Charts | ML 与图表价基一致；预测晚于订单不冒充当时可用；生命周期不足仅显示事实；不输出伪造假设收益 |
| W04 | 参数规范化、取消过时请求、缓存、按日期裁剪小时数据；界面回归 | 缓存键包含数据／预测集合及全部场景，不靠文件 mtime；红涨绿跌，键盘可用，移动端可读；测量冷热性能 |

参数处理不需要在开发阶段等待用户投资决策：测试可用现金 20,000、本金同币种、初始库存 0、US 每笔 10 股等明确标记的 fixture；HK 数量由 fixture 的 lot 元数据计算。至少验证零费用和正费用场景，正费用数值是合成测试值。面向用户的默认模式必须说明参数来源，不宣称其适合真实账户。

第四批必须交付已有模型也能使用的完整闭环。预测器是否晋级与库存策略是否设为默认分别判断；本单不进行生产默认切换。

## 7. 验证命令与证据

在**执行任务自己的工作树**运行。所有现有会读取真实路径的测试，要先确认已指向副本；新增测试优先用 tmp_path、合成行情、可注入时钟及 mock 网络／发布。

快速回归基线：

```bash
PYTHONDONTWRITEBYTECODE=1 /opt/anaconda3/envs/mk/bin/python -m pytest tests/ -q -p no:cacheprovider --ignore=tests/test_ml_offline_rl.py -k 'not backtest_survives_nan_close'
```

补齐有意义的测试文件，名称可随实现调整：`test_ml_sessions.py`、`test_ml_prediction_versions.py`、`test_ml_api.py`、`test_ml_execution.py`、`test_ml_pipeline_status.py`。覆盖本工单各项不变量，不保留错误行为只是为了让旧断言通过。

最终在数据副本上运行适用完整回归；若旧 RL 测试涉及缺失的可选依赖，明确记录，不能因此重启 RL 开发。禁止把测试中的 CLI 调用落到原目录或调用真实 `ml.sh all/publish`。

```bash
PYTHONDONTWRITEBYTECODE=1 /opt/anaconda3/envs/mk/bin/python -m pytest tests/ -q -p no:cacheprovider
node --check mystock/web/static/app.js
bash -n scripts/ml.sh
git diff --check
```

页面使用工作树数据和与现有服务不同的空闲端口。做正常／空库／部分缺口／非法参数／切换请求／移动端的浏览器验收；不能只以 JS 语法检查代替页面验证。性能目标参考方案：同机 6 股 120 session warm p95 <1s、cold <3s；不足时先报告实际测量与原因，再做必要优化，不为凑指标伪造数据。

回放验收至少有手算路径：现金 100 买入花 100、卖出收 110；现金虽日终不缺，盘中仍占用 100。再覆盖先卖无库存被拒绝、跳空、同 bar 双触达、费用、拆股／分红、缺行情、期末未平仓与多个窗口起始状态。

模型／策略成绩只写汇总，不提交个人交易明细、数据库、配置、HTML 私人报告、模型文件或含账户信息的截图。可提交使用合成数据的截图和实验摘要；本地详细结果留在 gitignored 输出目录。

## 8. 提交、进度与交接产物

- 在隔离分支按四批提交，合理拆分修复与实验，不混入与本单无关变更；保留用户 Git author／committer，每个提交末尾增加 `Co-authored-by: Codex <codex@openai.com>`。
- 持续更新 `docs/ML_UPGRADE_EXECUTION_LOG_2026-09-04.md`：任务 ID、状态、提交、测试命令及实际结果、数据／协议版本、失败与负结果、尚待前向验证事项。不得把此工单的“待实现”改写成未经验证的完成。
- 新增 `docs/ML_UPGRADE_EXPERIMENT_RESULTS_2026-09-04.md`：E0–E5 的实际矩阵、共同日期与样本数、各股指标、候选判定，以及固定策略费用／风险对照；没有运行的项目明确写未运行。
- 最终报告：已完成的任务 ID、分支／提交、测试与页面证据、可复现命令、生产切换与回滚步骤、尚未满足的真实 shadow／用户参数条件。
- 不自动合并 main；可以准备可审查的分支或 draft PR。若推送分支，先检查暂存范围，绝不提交备份／data／配置。原运行服务和备份保持原样。

完成四批工程与离线验证后交回用户审查即可。真实前向观察需在后续明确运行流程中逐日积累，不能靠重算历史、等待当前任务或自动建立定时器替代。
