# ML 升级部署回执 · 2026-09-05

> 文档身份：2026-09-05 · 原始作者 codex · 历史记录；2026-09-05 由 Codex 治理文件名与引用。作者／日期依据及旧名见文档清单。 [索引](../README.md) · [清单](../catalog.json)

用户确认 Claude 修复通过并授权合入 main、推送、重启本机 8888 和执行 ML 发布。工程合并提交为 `99848c9`，包含审核修复 `7d95fc7`；已推送 `origin/main`。README 冲突采用保留 main `885e8f7` 修订的审核版解决，合并代码树与审核版本一致。

## 实际执行

1. 在原运行目录 `data/deployments/20260905-ml-upgrade/` 保存切换前源码、配置和两库在线备份；复制恢复、integrity_check、schema 及版本幂等迁移演练通过。
2. 初始化运行库新 schema，294 条旧预测迁入审计版本；原主库持仓、订单、成交和行情内容核对不变。
3. 8888 服务已从 main 运行目录重启。首页、`/ml-next`、`/ml-next/` 均 HTTP 200，ML 数据连接只读验证通过。
4. 执行 `ml.sh data`，刷新 ML 日线、小时线和主库事实快照。没有额外执行 update.sh；本轮主库已有截至 9 月 4 日的账户事实，部署没有更改这些事实。
5. 运行库历史审计发现 2026-04-20 三只港股各缺完整小时线；使用此前核验的 Futu 原始 CSV，经导入器重新核对后补齐，保留 source_ref 和回执。重新逐日拟合 720 条历史预测，run `4d02a704680342c78452c2cbd9fdbcd0`，输入冻结在运行目录，训练／校准截止及 SHA256 核验通过。没有整库覆盖运行数据。
6. 执行 `ml.sh train`，run `deploy-20260905`，六股均 generated。备份公网旧 index 后执行 `ml.sh publish data/ml/receipts/deploy-20260905.json`，六股均 published，无过期发布。

## 验收

| 项目 | 结果 |
| --- | --- |
| 六股历史 20 / 60 / 120 session | 分别 120 / 360 / 720 条 ok，行情、预测与小时线无缺口 |
| 新 live 目标日 | HK：2026-09-07；US：2026-09-08 |
| 运行 ML 库 | integrity_check=ok；旧版本保留，recomputed 与 live 分开 |
| 本机页面 | 8888 首页及 ml-next 正常；历史默认包含明确标注的重建 |
| 公网报告 | HTTPS 200，返回内容 SHA256 与本次训练回执完全一致 |
| 工程验证 | 审核版 227 项测试已通过；合并代码树与审核版一致，本轮另完成真实迁移、数据、训练和发布验收 |

本机入口：[ML 页面](http://127.0.0.1:8888/ml-next/)。公网仅发布静态报告：[本次报告](https://g.ismayday.com/mystock/)，没有把本机账户 Web 暴露到公网。

私有备份、采集／训练／发布日志、历史导入证据、验证明细均在运行目录的 `data/deployments/20260905-ml-upgrade/`；训练回执位于 `data/ml/receipts/`，冻结输入与 manifest 位于 `data/ml/runs/`。这些文件、配置和真实账户数据不进入 Git。

此次为工程部署，未更换生产模型、未晋级 E0–E5 候选、未自动下单，也未配置自动任务。证券规则历史、输入快照保留、采集按市场部分失败等延期问题继续按 [Claude 修复回执](ml-upgrade-claude-review-fixes_codex_20260905.md) 跟踪。

## 同日补充：修复公网状态列表展示

基线 `e1e4263`，修复提交 `1502e9b`；在本地主目录 `/Users/kk/Work/Workpace/GitHub/Seattle/myStock` 的 main 直接修复。用户指出公网把 Python statuses 列表作为正文显示，属于原 P3-9 的状态展示部分；已改为按市场／生成状态／目标日分组的中文面板，技术设置折叠，字段转义。旧 bandit 报告口径仍按 OPEN_ITEMS 跟踪。

混合／跳过／未知／HTML 转义人工检查通过；6 项 pipeline 测试、文档校验通过。执行新 run `report-status-20260905` 的 train 和按回执 publish，六股均成功；公网 HTTPS 200、正文哈希与新产物一致，已核对不再出现原始状态列表。原报告及预测版本保留；没有改旧回执的哈希来绕过发布校验。

本轮直接在 main 提交并推送，没有额外分支合并；主库未写入，运行 ML 库只由 train 追加预测及日志、publish 记录实际发布时间；未重启 8888（静态报告发布不需要），未补采行情、未重算 720 条历史预测、未新增自动调度、未进行真实下单或交易。私有执行证据在 `data/deployments/20260905-report-status/`，未提交公开仓库。

## 同日补充：例行更新、训练和发布验收

基线 `b2a2e15`；在本地主目录 `/Users/kk/Work/Workpace/GitHub/Seattle/myStock` 的 main 完成。用户要求检查 `update.sh`、`ml.sh` 更新后的实际运行能力；沿用已授权的数据更新及公网发布范围。以下记录为真实运行结果，模型效果不属于本轮验收结论。

发现并修复：行情、资料及资金流向部分标的异常时，汇总仍记 `ok`；更新入口也始终返回成功。现在部分异常汇总记 `error`，不推进对应来源的成功同步点；入口检查本轮新增错误日志，列出失败来源并返回 1。独立来源继续采集，已写入的成功数据保留，历史错误不影响成功重试。空结果和跳过名单仍采用原有口径，不因周末无增量而误报异常。

执行前完成主库和 ML 库在线备份、备份完整性检查及公网旧报告备份。先实跑原入口，再实跑修复后的 `bash scripts/update.sh`，最后使用默认 conda `mk` 环境执行 `MYSTOCK_ML_RUN_ID=routine-check-20260905T145032 bash scripts/ml.sh all`；没有通过 Python 路径覆盖绕过脚本的环境激活。

| 验收项 | 实际结果 |
| --- | --- |
| 主库例行更新 | 两轮退出码均为 0；九类数据来源日志全部 `ok`。首轮 40 个标的日线、资料、盘面及资金流向均成功；第二轮当日没有新增订单、成交和资金流向，正常返回空增量 |
| ML 采集 | `data_complete`，六股均 ready；失败与预警均为 0 |
| 训练与预测 | 六股均 generated；HK 目标 2026-09-07，US 目标 2026-09-08 |
| 公网发布 | 2026-09-05 14:57:18 UTC 发布，六条版本均 published；HTTPS 200，正文 SHA256 与回执和冻结报告一致，未出现原始状态列表 |
| 本机读取 | 首页、ml-next 和 latest API 均 HTTP 200；API 六股均返回本次 run 的 published 版本 |
| 历史完整性 | 截至 2026-09-04，重建视图近 20 / 60 / 120 个交易日分别 120 / 360 / 720 条 `ok`；没有补采或重算历史预测的必要 |
| 数据审计 | 两库 integrity_check 均 ok；备份中所有旧预测版本逐行核对不变 |
| 回归验证 | conda mk 中 41 项相关测试通过，包含新增部分失败、同步点保留、入口失败退出及旧错误不污染重试；ML 测试使用临时数据库和模拟外部调用 |

后续人工例行运行可使用：

```bash
bash scripts/update.sh && bash scripts/ml.sh all
```

这条命令包含公网发布；主库或 ML 采集异常会阻止后续步骤。`ml.sh` 无参数只显示帮助，独立发布仍必须指定训练回执；盘中跳过、发布截止和哈希校验规则继续生效。此次刷新已完成，无须立即再跑一次。

本轮在 main 直接修复，无额外分支合并；提交／推送以对应 Git 提交为准。主库写入来自例行采集，ML 库写入来自采集／训练／发布管线；未重启 8888、未更换生产模型、未配置自动调度、未进行真实下单或交易。既有延期项仍见 [未尽事项](../OPEN_ITEMS.md)。私有备份、两轮更新日志、ML 日志、测试日志及 `verification.json` 在 `data/deployments/routine-check-20260905T145032/`，不提交运行数据。已有其他任务的 Web 文档改动保持原样，不纳入本次提交。
