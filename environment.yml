name: mk
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - futu-api==10.10.7008
      - yfinance==1.7.0
      - Flask>=3.0.0
      - PyYAML>=6.0
      - pandas>=2.0.0
      # ML（mystock/ml/，可选；缺失时相关模块 graceful skip）
      - scikit-learn>=1.3.0
      - lightgbm>=4.0.0
      - scipy>=1.10.0
      # ML 实验后端（2026-09-06 经用户授权加入 mk；均为可选导入，缺失时实验脚本明确失败）
      - catboost==1.2.10
      - xgboost==3.2.0
      - arch==8.0.0
