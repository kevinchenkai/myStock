name: mk
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - futu-api>=7.0.0
      - yfinance>=0.2.40
      - Flask>=3.0.0
      - PyYAML>=6.0
      - pandas>=2.0.0
      # ML（mystock/ml/，可选；缺失时相关模块 graceful skip）
      - scikit-learn>=1.3.0
      - lightgbm>=4.0.0
      - scipy>=1.10.0
